services:
  bot:
    build: .
    image: wow-assistant:0.1.0
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot-data:/app/data
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true

volumes:
  bot-data:
